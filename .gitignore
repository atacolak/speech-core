/target/
/logs/
/result
/result-*
.direnv/
.env
*.log
*.pid
*.tmp
.DS_Store

.pi/

# Golden suite run artifacts (local recordings, rejected takes, raw captures)
/tests/golden/runs/
/tests/golden/legacy/recordings/

# Python
__pycache__/
*.pyc
.pytest_cache/

# bv (beads viewer) local config and caches
.bv/

# Pi terminal settlement checkpoints (local session artifacts)
.checkpoints/

# Beads runtime (ledger export lives under .beads/ with its own ignore)
.beads/*.db
.beads/*.db-*
.beads/*.lock
.beads/last-touched
.beads/.br_history/
.beads/.br_recovery/
