/target/
/logs/
/result
/result-*
.direnv/
.env
*.log
*.pid
*.tmp
.DS_Store

# Repo-local agent/session memory is runtime residue, not project truth.
.pi/

# Golden suite run artifacts (local recordings, rejected takes, raw captures)
/tests/golden/runs/
/tests/golden/legacy/recordings/

# Python
__pycache__/
*.pyc
.pytest_cache/

# bv (beads viewer) local config and caches
.bv/

# Pi terminal settlement checkpoints (local session artifacts)
.checkpoints/

# City work-ledger runtime. The tracked export/config created by rig registration
# remains visible; local database/backend/cache files do not.
.beads/*.db
.beads/*.db-*
.beads/*.lock
.beads/last-touched
.beads/.local_version
.beads/.auto-import-issues.jsonl
.beads/.br_history/
.beads/.br_recovery/
.beads/backup/
.beads/embeddeddolt/
