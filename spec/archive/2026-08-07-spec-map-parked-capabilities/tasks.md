# tasks.md — spec-map-parked-capabilities

Living checklist. Downstream docs edit only; steward drafts and verifies,
workers execute. Spawn only after operator accept + factory path.

- [x] Edit `spec/README.md`: insert the **not yet accepted (parked / direction)**
      table and the one-sentence honesty note from `proposal.md`, after the
      accepted-chapters TOC and before the archive TOC.
- [x] Confirm no other file changes (diff is README-only).
- [x] Steward (post-delivery): re-read flow `speech-in.md` pointer against the
      new map row; archive this change when converged.
