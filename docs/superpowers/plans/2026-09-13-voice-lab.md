# Voice lab workbench Implementation Plan

**Goal:** Replace the single-reference editor with a voice-profile workbench: many sources, experiments, references; AuK XOR Breeze audition.

**Architecture:** migrate sqlite off 1:1 `source_artifact_id`; `voice_artifacts` with role experiment|reference; workbench UI; staged audition via existing ProcessorLease.

**Spec:** `docs/superpowers/specs/2026-09-13-voice-lab.md`
**Brief:** `docs/superpowers/briefs/2026-09-13-voice-lab.md`

t3 (`sc-auk-lab-yzvf.3`) remains the isolated AuK runtime pin. This epic consumes it; do not steal that bead.

---

### Task 1: Schema — sources / artifacts / auditions

**Owner:** builder
**Files:** `schema.sql`, `db.py` migrate, `models.py`, voice JSON, tests
**Verification:** a voice can have 2 sources + 2 experiments + 2 references; original bytes immutable; parent lineage; existing voices migrate (current source → one source, approved auk → reference, other auk → experiment). No GPU. No UI.

**Landed 2026-09-13:** `voice_sources` / `voice_artifacts` / `breeze_auditions`. Additive JSON. MAIN 40 python.


### Task 2: Workbench shell (replace modal)

**Owner:** builder
**May parallelize with t1** if it talks to today's 1:1 API and swaps to multi-source once t1 lands. Prefer: scaffold UI against the t1 JSON shape, with a fake two-source fixture in tests.
**Files:** replace `reference-editor.tsx` modal with a workbench occupying the lab (assets / workbench / inspector / compare). Kill "Edit reference" overlay. Intent modes regroup `auk-tasks.ts`. Hide precision radios from the creative row.
**Verification:** vitest: no Analyze speakers / resemble / qwentts; assets lists sources vs references vs experiments; compare strip present; Done still closes. No GPU.

**Landed 2026-09-13:** `VoiceWorkbench` replaces the Edit-reference modal. Intent modes CLEAN|ISOLATE|PERFORM|EDIT|SYNTHESIZE. Precision behind diagnostics. MAIN 50 vitest.

### Task 3: Multi-candidate generate + compare

**Owner:** builder
**Depends:** t1, t2, and t3 runtime routes (`/api/auk` or `/api/voices/{id}/auk`)
**Verification:** generate N=2–3; A/B on cards; approve promotes to reference; playground can pick among references.

### Task 4: Staged Breeze audition

**Owner:** builder
**Depends:** t1, t3 runtime, t3 auk XOR breeze lease
**Verification:** audition unloads auk, loads breeze, writes take onto the artifact, survives swapping back. 409 on live call. leftover stays parked.

### Parked

- revive vibevoice as timeline diarization
- resemble vs auk enhance bakeoff
- leftover env rename (`sc-leftover-instruct-ykz1`)
- automatic delivery-based reference pick

### Frontier now

Mint t1 (schema) and t2 (shell) together. t3/t4 of this epic wait on auk runtime `sc-auk-lab-yzvf.3`.
