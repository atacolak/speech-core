# AuK lab Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Make AuK the offline reference refinery upstream of Breeze TTS2 on the local 12 GB 4070, park VibeVoice, demote Resemble from the default path, and scrub dead leftover names from lab chrome.

**Architecture:** Separate AuK venv + subprocess behind the existing GPU lease. Preferred pin `auk_base_bf16 + qwen_omni_w4a8 + fp32 VAE`. Candidates are lineage-bearing artifacts. Breeze consumes approved refs only. No ComfyUI.

**Tech stack:** FastAPI lab, sqlite artifacts, React reference editor, official AuK inference behaviour, Saganaki22/drbaph quantized checkpoints without `comfy.*`.

**Spec:** `docs/superpowers/specs/2026-09-12-auk-lab.md`
**Brief:** `docs/superpowers/briefs/2026-09-12-auk-lab.md`

---

### Task 1: Dead-name polish + park VibeVoice/Resemble from default UI

**Owner:** builder
**Files:**
- Modify: `tts/README.md`, `tts/docs/architecture.md`, `tts/__init__.py`
- Modify: `tts/lab/web/src/features/voices/reference-editor.tsx`
- Modify: `tts/lab/web/src/features/voices/voices-pane.tsx`
- Modify: `tts/lab/web/src/features/inspector/inspector-pane.tsx`
- Modify: `tts/lab/web/src/features/runtime/runtime-indicator.tsx` (only if leftover-parked / not-a-pin-swap chrome remains)
- Modify: `tts/lab/backend/routes/leftover.py` comments
- Modify: `tts/lab/backend/store/session.py` comments
- Modify: `tts/lab/backend/services/talker.py` comments
- Test: `tts/lab/web/src/features/voices/reference-editor.test.tsx`
- Test: `tts/lab/web/src/features/voices/pane-reference.test.tsx`
- Do **not** edit `speech-out.env` or leftover binary keys
- Do **not** delete `speakers.py` / `resemble.py` / their unittests

**Verification:** default editor has no Analyze speakers, no Resemble denoise radios, no stream.fm / qwentts / cosyvoice operator strings. Desk `GET /api/runtime` still includes existing JSON keys. Speaker/resemble unittests still pass.

**Landed 2026-09-12:** editor unmounted speaker/cleanup chrome; `resemble` operator word is `denoised`; leftover hop wording in tts docs; env keys untouched. MAIN: 58 python + 13 vitest. dist rebuilt (`index-Ci_qf7uz.js`) with no parked strings; lab-web not bounced (talker still holds Breeze).

### Task 2: Artifact lineage for AuK candidates

**Owner:** builder
**Files:**
- Modify: `tts/lab/backend/models.py` — `kind` includes `auk`; candidate provenance fields
- Modify: `tts/lab/backend/store/schema.sql` + `db.py` migrate
- Modify: `tts/lab/backend/store/cache.py` if cache key must include task/precision/seed
- Test: new `tts/lab/backend/tests/test_auk_artifacts.py` (no GPU)

**Verification:** creating an AuK candidate does not mutate source; parent_id / task / instruction / precision / seed persist; approve marks it Breeze-eligible; keep change stales derived auk rows the same way resemble already stales.

**Landed 2026-09-12:** `kind=auk` on existing `reference_variants` (no second store). Provenance columns + `_migrate`. `record_auk_candidate` / `set_candidate_approved`. Cache forks on task/instruction/pin/precision/seed/settings/parent/keep; resemble hashes frozen (`3ea87f59…`). MAIN: 44 python OK. Isolated verifier stream-died; lead re-ran the suite. Talker still holds Breeze; t3 not minted.

### Task 3: Isolated AuK runtime (bf16 + w4a8 + fp32 VAE)

**Owner:** builder
**Depends on:** weights download (may be this task)
**Files:**
- Create: `tts/lab/backend/runtime/auk.py` (or similar) + worker
- Create: separate venv under `~/.local/share/speech-out/auk-base/`
- Port quantized load **without** `import comfy`
- Wire `ProcessorLease` occupant `auk`
- HTTP: `/api/auk/runtime` load/unload
- Test: fake-worker occupancy tests in `test_processors_lease.py` style; no GPU in CI

**Verification:** load parks leftover, unloads Breeze, `restore_leftover=False`. live_call → 409. missing weights → 501. encoder/model/vae can be dropped independently (even if first impl loads all three, the API must not require them immortal). ComfyUI is not a dependency of speech-core.

**GPU evidence (this machine):** preferred pin actually loads on the 4070; record peak MiB for a short enhance. int8 pin may be downloaded but must not auto-select.

**Landed 2026-09-13 (occupancy, not generate):** pin + venv + AukRuntimeManager + `/api/auk/runtime` load/unload/offload. Real 4070 load 17.5s, nvidia-smi 326→7318 MiB, VAE offload −588 MiB, leftover parked. `POST /api/voices/{id}/auk` is 501 `engine_unavailable` until sampler port (`sc-auk-lab-yzvf.6`). MAIN: 48 python OK. No silent int8. No `import comfy` in speech-core.

**Sampler t6 (`sc-auk-lab-yzvf.5`) landed 2026-09-13:** Flux2Edit + Qwen encoder + BigVGAN in the AuK venv. Live enhance on `vp_sample_george_hotz` 200, kind=auk, source unchanged, generate peak 9016 MiB, unload back to 326, leftover parked. MAIN 35 python.

### Task 4: Task presets + generate candidate + A/B editor

**Owner:** builder
**Depends on:** Task 2, Task 3
**Files:**
- Modify: `reference-editor.tsx` SOURCE → PROCESS → CANDIDATES
- Modify: `tts/lab/web/src/lib/api.ts`
- Create: instruction templates from official cookbook (enhance/denoise/repair/separate/clone/volume first)
- Lower-confidence tasks present but marked
- Instant A/B playback, no download
- Advanced nfe/cfg/sway behind debug

**Verification:** drop audio, crop, pick enhance, generate, A/B vs source without leaving the editor. Provenance visible. No knob pile on the main surface.

**Landed 2026-09-13:** editor PROCESS (cookbook chips + instruction + bf16/int8 + collapsed nfe/cfg/sway) and CANDIDATES (A/B AudioBars, provenance, approve→activate). Client against frozen `/api/auk/*`. MAIN: 28 vitest. Generate 404s until t3 routes exist. lab-web not bounced.

### Task 5: Approve into Breeze + survival matrix

**Owner:** builder
**Depends on:** Task 4
**Files:**
- Approve endpoint + Breeze/talker clone-from-auk-artifact (same as current non-original variant path)
- Qual note: `docs/qualification/auk-breeze-survival.md` (or similar)
- Five refs: neutral/warm/angry/soft/high-energy; identical Breeze text+steer
- bf16 vs int8 A/B on enhance + separate at least

**Verification:** approved AuK wav is what Breeze clones. Written listen notes on identity vs performance wash. int8 never silently replaces bf16.

### Parked (do not mint until measured)

- Resemble vs AuK enhance fidelity bakeoff
- Revive VibeVoice diarization if AuK isolate is not enough
- AuK-Flash preview path
- mossformer / TSE / streaming vibevoice
- leftover env rename (`sc-leftover-instruct-ykz1`)

### Frontier

Mint only Task 1 now (no GPU, unblocks the lab surface). Task 2+3 after t1; they can parallelize (schema vs runtime) if they do not share files. Task 4 waits on both. Task 5 waits on 4.
